# Copy to .env for local testing with `netlify dev`, or set in Netlify → Site settings → Environment variables.

# Username and password for dashboard access
SITE_USER=your_username
SITE_PASSWORD=your_secure_password

# Random secret for signing session cookies (e.g. openssl rand -hex 32)
AUTH_SECRET=change_me_to_a_long_random_string
